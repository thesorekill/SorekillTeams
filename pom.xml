<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="
           http://maven.apache.org/POM/4.0.0
           http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>net.chumbucket</groupId>
  <artifactId>SorekillTeams</artifactId>
  <version>1.2.1</version>
  <packaging>jar</packaging>
  <name>SorekillTeams</name>

  <!-- ========================================================= -->
  <!-- Properties -->
  <!-- ========================================================= -->
  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Paper / Spigot -->
    <spigot.api.version>1.21.4-R0.1-SNAPSHOT</spigot.api.version>

    <!-- Optional integrations -->
    <placeholderapi.version>2.11.7</placeholderapi.version>
    <miniplaceholders.version>3.1.0</miniplaceholders.version>

    <!-- Build plugin versions -->
    <maven.compiler.plugin.version>3.13.0</maven.compiler.plugin.version>
    <maven.shade.plugin.version>3.6.0</maven.shade.plugin.version>
    <maven.jar.plugin.version>3.4.2</maven.jar.plugin.version>

    <!-- Runtime SQL pool (shaded) -->
    <hikari.version>5.1.0</hikari.version>

    <!-- Libby (runtime dependency loader) -->
    <libby.version>1.3.1</libby.version>

    <!-- ✅ Redis client (NOT shaded; downloaded at runtime via Libby) -->
    <jedis.version>5.2.0</jedis.version>

    <!-- JDBC driver versions (NOT shaded; downloaded at runtime via Libby) -->
    <mysql.driver.version>9.1.0</mysql.driver.version>
    <mariadb.driver.version>3.5.0</mariadb.driver.version>
    <postgres.driver.version>42.7.4</postgres.driver.version>
    <sqlite.driver.version>3.46.1.0</sqlite.driver.version>
    <h2.version>2.3.232</h2.version>
  </properties>

  <!-- ========================================================= -->
  <!-- Repositories -->
  <!-- ========================================================= -->
  <repositories>

    <!-- Spigot snapshots -->
    <repository>
      <id>spigotmc-repo</id>
      <url>https://hub.spigotmc.org/nexus/content/repositories/snapshots/</url>
    </repository>

    <!-- PlaceholderAPI -->
    <repository>
      <id>extendedclip-placeholderapi</id>
      <url>https://repo.extendedclip.com/content/repositories/placeholderapi/</url>
    </repository>

    <!-- MiniPlaceholders -->
    <repository>
      <id>miniplaceholders</id>
      <url>https://repo.minebench.de/</url>
    </repository>

    <!-- Libby lives here (AlessioDP repo) -->
    <repository>
      <id>alessiodp-repo</id>
      <url>https://repo.alessiodp.com/releases/</url>
    </repository>

    <!-- Maven Central -->
    <repository>
      <id>maven-central</id>
      <url>https://repo1.maven.org/maven2/</url>
    </repository>

  </repositories>

  <!-- ========================================================= -->
  <!-- Dependencies -->
  <!-- ========================================================= -->
  <dependencies>

    <!-- Spigot API (Paper-compatible) -->
    <dependency>
      <groupId>org.spigotmc</groupId>
      <artifactId>spigot-api</artifactId>
      <version>${spigot.api.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- PlaceholderAPI (optional, runtime-provided) -->
    <dependency>
      <groupId>me.clip</groupId>
      <artifactId>placeholderapi</artifactId>
      <version>${placeholderapi.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- MiniPlaceholders (optional, runtime-provided) -->
    <dependency>
      <groupId>io.github.miniplaceholders</groupId>
      <artifactId>miniplaceholders-api</artifactId>
      <version>${miniplaceholders.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- HikariCP (shaded + relocated) -->
    <dependency>
      <groupId>com.zaxxer</groupId>
      <artifactId>HikariCP</artifactId>
      <version>${hikari.version}</version>
      <exclusions>
        <exclusion>
          <groupId>org.slf4j</groupId>
          <artifactId>slf4j-api</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!-- Libby (runtime loader) -->
    <dependency>
      <groupId>net.byteflux</groupId>
      <artifactId>libby-bukkit</artifactId>
      <version>${libby.version}</version>
    </dependency>

    <dependency>
      <groupId>net.byteflux</groupId>
      <artifactId>libby-core</artifactId>
      <version>${libby.version}</version>
    </dependency>

    <!-- ✅ Redis client (COMPILE-ONLY / NOT SHADED)
         We compile against Jedis, but DO NOT ship it in the jar.
         Load it at runtime with Libby exactly like your JDBC drivers. -->
    <dependency>
      <groupId>redis.clients</groupId>
      <artifactId>jedis</artifactId>
      <version>${jedis.version}</version>
      <scope>provided</scope>
      <exclusions>
        <!-- keep your jar clean; server supplies logging, or you don't need it -->
        <exclusion>
          <groupId>org.slf4j</groupId>
          <artifactId>slf4j-api</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

    <!--
      IMPORTANT:
      JDBC drivers are intentionally NOT dependencies anymore.
      They will be downloaded at runtime based on storage.type:
      - mysql:      com.mysql:mysql-connector-j:${mysql.driver.version}
      - mariadb:    org.mariadb.jdbc:mariadb-java-client:${mariadb.driver.version}
      - postgresql: org.postgresql:postgresql:${postgres.driver.version}
      - sqlite:     org.xerial:sqlite-jdbc:${sqlite.driver.version}
      - h2:         com.h2database:h2:${h2.version}

      ✅ Redis client is also intended to be runtime-loaded via Libby:
      - jedis: redis.clients:jedis:${jedis.version}
    -->

  </dependencies>

  <!-- ========================================================= -->
  <!-- Build -->
  <!-- ========================================================= -->
  <build>

    <finalName>${project.artifactId}-${project.version}</finalName>

    <!-- Resource filtering:
         - ONLY plugin.yml is filtered (version injection)
         - All other YAML files remain untouched -->
    <resources>
      <resource>
        <directory>src/main/resources</directory>
        <filtering>false</filtering>
        <excludes>
          <exclude>plugin.yml</exclude>
        </excludes>
      </resource>

      <resource>
        <directory>src/main/resources</directory>
        <filtering>true</filtering>
        <includes>
          <include>plugin.yml</include>
        </includes>
      </resource>
    </resources>

    <plugins>

      <!-- Java 21 compiler -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${maven.compiler.plugin.version}</version>
        <configuration>
          <release>${java.version}</release>
          <encoding>${project.build.sourceEncoding}</encoding>
        </configuration>
      </plugin>

      <!-- Shade only small embedded libs (Hikari + Libby) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven.shade.plugin.version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>

              <createDependencyReducedPom>false</createDependencyReducedPom>
              <minimizeJar>false</minimizeJar>

              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>

                    <exclude>module-info.class</exclude>
                    <exclude>META-INF/versions/**/module-info.class</exclude>

                    <exclude>META-INF/LICENSE*</exclude>
                    <exclude>META-INF/NOTICE*</exclude>
                    <exclude>META-INF/DEPENDENCIES*</exclude>
                    <exclude>META-INF/AL2.0</exclude>
                    <exclude>META-INF/LGPL2.1</exclude>
                  </excludes>
                </filter>
              </filters>

              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
              </transformers>

              <relocations>
                <relocation>
                  <pattern>com.zaxxer.hikari</pattern>
                  <shadedPattern>net.chumbucket.sorekillteams.libs.hikari</shadedPattern>
                </relocation>

                <!-- Libby packages are net.byteflux.libby.* -->
                <relocation>
                  <pattern>net.byteflux.libby</pattern>
                  <shadedPattern>net.chumbucket.sorekillteams.libs.libby</shadedPattern>
                </relocation>

                <relocation>
                  <pattern>me.lucko.jarrelocator</pattern>
                  <shadedPattern>net.chumbucket.sorekillteams.libs.jarrelocator</shadedPattern>
                </relocation>
              </relocations>

            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Jar manifest -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>${maven.jar.plugin.version}</version>
        <configuration>
          <archive>
            <manifest>
              <addDefaultImplementationEntries>true</addDefaultImplementationEntries>
              <addDefaultSpecificationEntries>true</addDefaultSpecificationEntries>
            </manifest>
          </archive>
        </configuration>
      </plugin>

    </plugins>

  </build>

</project>
